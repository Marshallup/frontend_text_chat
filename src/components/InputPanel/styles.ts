import styled from "styled-components";
import { Button } from "@mui/material";

export const InputPanelForm = styled.form`

`;
export const InputPanelInner = styled.div`
    display: flex;
    align-items: flex-end;
`;
export const SendBtn = styled(Button)`
    
`;